# FastAPI backend URL — used by both Vite dev proxy and Express server.ts
# Default is localhost:8000. Override if backend runs elsewhere.
VITE_API_URL=http://localhost:8000
FASTAPI_URL=http://localhost:8000
